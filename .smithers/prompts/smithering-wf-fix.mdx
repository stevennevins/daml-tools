# Fix the workflow until its graph renders

The generated workflow at .smithers/workflows/smithering-impl.tsx fails to render its
graph.

## Errors

{props.errors}

Make the smallest change that fixes the error without abandoning the orchestration design.
Do not delete the workflow or stub it out to "pass" — fix the real problem. Re-run
"bunx smithers-orchestrator graph .smithers/workflows/smithering-impl.tsx" yourself
before finishing.
