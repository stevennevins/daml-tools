# Brainstorm

Think really hard about this request and map the problem space.

- Request: {props.prompt}
- Intake: {props.intakeSummary} (type: {props.productType}, {props.classification}, target: {props.targetRepo})
- Constraints: {props.constraints}

Produce: a crisp problem statement, the core capabilities a 10/10 version needs, the real
risks, and OPEN QUESTIONS. Question discipline: focus on PRODUCT questions (who is it for,
what does success look like, scope boundaries), not implementation details. For every
question include your recommended answer and why it matters — downstream steps auto-adopt
recommendations when the human is unavailable.

Write the full brainstorm to artifacts/smithering/brainstorm.md and set artifactPath.
