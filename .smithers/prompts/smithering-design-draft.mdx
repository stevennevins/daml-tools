# Design doc draft

Write (or revise) the design doc draft at docs/planning/02-design-draft.md.

Read: docs/planning/01-prd.md and artifacts/smithering/research/design-art.md.

{props.feedback ? `Apply this reviewer feedback before anything else:
${props.feedback}
Issues: ${props.issues}` : ""}

The design doc digs into granular details the PRD does not: layout and design patterns for
UIs, API nuances for APIs, ergonomics for libraries. High emphasis on pleasing the end
user. Record each significant decision with rationale (decisions[]).
