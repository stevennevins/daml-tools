# Adversarial cross-model review

Adversarial cross-model review of the built product in {props.targetRepo} (set
reviewer="codex").

Focus: correctness, security, architecture conformance to docs/planning/03-eng.md, test
vacuousness (tests that cannot fail, over-mocked e2e), and error handling. Read the diffs
and the tests — do not trust summaries.
