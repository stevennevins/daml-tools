# Orchestrator review

Orchestrator review of the built product in {props.targetRepo} (set reviewer="fable").

Focus on what matters most: does the product satisfy docs/planning/01-prd.md, and does
EVIDENCE exist for every blocking gate in docs/planning/04-backpressure.md (check
artifacts/smithering/build/)? Missing evidence for a blocking criterion is automatically
a major issue — "the agent said it was done" does not count. Also check
artifacts/smithering/decisions/ — significant judgment calls without a decision doc are a
minor issue each.
