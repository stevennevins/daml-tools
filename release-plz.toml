# release-plz configuration — https://release-plz.dev/docs/config
#
# Each crate is versioned, changelogged, tagged, and published INDEPENDENTLY
# from its own conventional-commit history (the parser stays boring and stable;
# the two CLIs bump faster). release-plz publishes in dependency order, so
# daml-parser reaches crates.io before daml-lint and daml-fmt.

[workspace]
# Maintain a CHANGELOG.md per crate from conventional commits.
changelog_update = true
# Tag and cut a GitHub Release per crate version.
git_tag_enable = true
git_release_enable = true
# Don't run `cargo update` when preparing release PRs; keep the committed
# Cargo.lock as-is and leave external dependency bumps to dependabot.
dependencies_update = false

[[package]]
name = "daml-parser"
# Public library on strict SemVer. release-plz uses cargo-semver-checks to
# classify API compatibility and choose the right version bump once a crates.io
# baseline exists; CI runs the same check as a second signal.
semver_check = true

[[package]]
name = "daml-lint"
# Publishes both a CLI and a library surface for detector/IR integrations; check
# that public API so release-plz can pick the right version bump.
semver_check = true

[[package]]
name = "daml-fmt"
# Publishes both the CLI and the `daml_fmt` library entry points used by
# integrations; check that public API too.
semver_check = true
